{
    const prompt = require('prompt-sync')();
    
            var size = prompt("Enter the size");
            var text = prompt("Enter the text");
        function make_shirt(){
          console.log(size);
          console.log(text);  
    }
     make_shirt();
}